# Decision Log

このファイルは、antEX の設計判断を記録するためのログである。

単なる実装履歴ではなく、ゲームの方向性に影響する判断と、その結果として採用または削除したものを残す。

## 2026-06-22: リポジトリを antEX として分離

### Decision

前バージョンのリポジトリとは分けて、新しく `antEX` として開発する。

### Result

- 既存の別リポジトリは前の状態に戻す対象とした
- antEX は新しい方向性の実験と実装を受け持つ
- 公開先は `https://github.com/yohei2000/antEX`

## 2026-06-22: メインコンセプトを放置ゲームへ変更

### Decision

antEX の中心コンセプトを「アリの巣の大帝国を目指す放置ゲーム」にする。

### Result

- 水、枝、物、餌を置いて反応を見るラボ型機能は主軸から外した
- 時間経過による食料増加、アリ増加、巣の成長を中心にした
- プレイヤーの主な判断は、強化と遠征に寄せる

## 2026-06-22: 初期アリ数を少なくする

### Decision

最初から大量のアリを出さない。

### Result

- 初期アリ数は 12 匹程度にした
- 盤面上の表示数には上限を設ける
- 小さな巣から始まる成長感を優先する

## 2026-06-22: アリの見た目を小さくする

### Decision

アリが大きすぎて不快に見える状態を避ける。

### Result

- アリの表示スケールを控えめにする
- 画面上では個体の細部より、群れとしての動きを重視する
- 頭、胸、腹、触角、6 本脚のシルエットは維持する

## 2026-06-22: マップを広げ、自然環境を入れる

### Decision

巣の周辺だけでなく、広めの地形と複数の餌場を持つ自然環境にする。

### Result

- ワールド半径を広めに設定した
- 複数の餌場を配置した
- 地形パッチによって盤面に変化を出した
- 採餌ルートと危険地帯が生まれる方向にした

## 2026-06-22: 水や枝の配置ツールを削除

### Decision

水、枝、物などを置いてアリの反応を見る機能は削除する。

### Result

- 下部ツールバー型の観察ラボ操作は廃止した
- UI は成長、強化、遠征に寄せた
- プレイヤーの目的を「反応を見る」から「巣を育てる」に明確化した

## 2026-06-22: 外敵として敵対アリを追加

### Decision

外敵は、見た目の違う少数のアリとして配置する。

### Result

- 敵対アリは味方より少し大きい
- 色は茶色系にする
- ランダムに配置される
- 餌場に寄りやすく、味方の採餌を邪魔する

## 2026-06-22: 接触時は個体パラメータで押し合う

### Decision

味方アリと敵対アリが接触した場合、単純な固定勝敗ではなく、個体パラメータで優先される側が相手を弾く。

### Result

- 味方側は役割、慎重さ、粘り強さを使う
- 敵対側は攻撃性、しぶとさ、体格を使う
- 負けた味方アリはパニック化し、採餌行動を中断される
- 外敵は盤面上の採餌効率を下げる環境要素になった

## 2026-06-22: worldbuilding.md を削除

### Decision

`docs/worldbuilding.md` は `docs/design-pillars.md` と内容が重複するため削除する。

### Result

- 世界観と設計方針は `docs/design-pillars.md` に集約する
- 判断の履歴は `docs/decision-log.md` に集約する
- docs の役割分担を単純化した

## 2026-06-22: 敵アリ戦闘を即時ノックバックから短い交戦へ変更

### Decision

敵アリとの接触は一瞬で勝敗を出さず、約2秒の組み合いを見せてから、個体パラメータに基づいて敗者が巣またはホーム地点へ逃げ帰る形にする。

### Result

- 勝敗計算の根拠は既存の役割、粘り強さ、警戒心、敵アリの攻撃性を維持する
- 大きな即時押し出しをなくし、接触直後は小さな位置調整だけにする
- 敵アリ色は明るい橙から低彩度の赤茶へ変更し、自然環境内で浮きすぎないようにする
- スマホでは管理パネルをスワイプで折り畳み、アリの動きを見られる面積を確保する

## 2026-06-23: 喧嘩中の回転を抑え、カメラズームを追加

### Decision

敵アリとの交戦中は、アンカー地点の周囲をくるくる回るのではなく、その場で組み合っているように見せる。観察性を上げるため、スマホは二本指ピンチ、PCはマウスホイールでカメラ距離を変えられるようにする。

### Result

- 交戦中の位置表現は固定した組み合い軸と小さな揺れに寄せる
- 勝敗計算、交戦時間、敗者逃走、セーブデータ形式は変更しない
- カメラズームは既存のカメラ距離だけを変え、盤面のゲーム進行には影響させない
- Playwright と verify で、喧嘩中の中心位置・向きの変化とズーム操作を確認する

## 2026-06-23: 巣の成長強化をツリー化

### Decision

強化を単純な6項目から、実際のアリの巣で観察される機能に近い枝へ分ける。モデル化する軸は、採餌網、育房、巣構造、防衛の4系統にする。

### Basis

- 働き蟻は採餌、育児、巣作り、防衛を担うため、強化もその社会機能へ対応させる
- 幼虫と蛹は安定した温度の育房に移されるため、育房と保温を孵化速度へ対応させる
- 採餌は帰巣する個体の接触や匂い道で調整されるため、採餌道と匂い道を採餌効率へ対応させる
- 巣は地下の坑道、部屋、換気、廃棄場所を持つため、坑道分岐、換気孔、廃棄坑を収容上限、回復、防御へ対応させる

Sources:
- https://en.wikipedia.org/wiki/Ant
- https://en.wikipedia.org/wiki/Red_harvester_ant
- https://arxiv.org/abs/2107.11075
- https://press.princeton.edu/books/hardcover/9780691179315/ant-architecture

### Result

- 強化ノードを13個に増やした
- 旧強化IDは維持し、新しい強化IDはセーブ読み込み時に0で補う
- 深いノードは前提Lvを必要にし、序盤から全効果を一気に買えないようにした
- 最大強化時でも採餌、孵化、収容、防衛が極端に跳ねない範囲をPlaywrightとverifyで検証する

## 2026-06-23: 巣の入口と自然地形を強化

### Decision

巣は平たい黒丸ではなく、斜面に開いた入口、土の縁、掘り出し土で表現する。フィールドは色パッチだけにせず、低い土塊、自然石、細い枝を置いて、採餌を完全に妨げない範囲で地面の凹凸と障害物を読む体験を足す。

### Result

- 巣入口を 4 か所にし、入口ごとに暗い穴、縁、掘り出し土を持たせた
- 巣レベル成長時は入口と掘り出し土も基準値から再配置し、スケールがフレームごとに累積しないようにした
- フィールドに低い土塊 10、自然石 6、枝 5 を固定配置した
- 自然石は衝撃リングを持たない静的障害物とし、既存のプレイヤー操作由来の石とは表示更新を分けた
- セーブデータ形式と強化/戦闘/ズーム仕様は変更しない

## 2026-06-23: 遠征戦闘を個体エージェント物理へ移行開始

### Decision

遠征戦闘の勝敗を、旧来の戦力比率と乱数による勝率抽選から切り離し、個体アリの接触、向き、押し合い、噛みつき、士気低下、撤退から決める agent engine へ移す。既存の通常盤面と保存形式は変更しない。

### Result

- `src/expedition/agent/` に deterministic RNG、spatial hash、AntAgent 型、固定ステップ個体物理、状態機械、metrics、adapter、InstancedMesh renderer の基礎を追加した
- `startExpedition()` は `runExpeditionAgentBattle()` の結果を使い、成功/失敗、負傷、報酬、脅威変化、診断を agent battle 由来にした
- 遠征UIの勝率表示は確率値ではなく、個体戦エンジンの表示に変更した
- 遠征開始時に agent battle の frame log を replay として通常3D scene上に InstancedMesh 表示する
- `agent` を既定の遠征engine設定として明示し、旧群モデルは `src/expedition/legacy/` に隔離して `expeditionEngine=legacy` の場合だけ使用する
- `recoil` と `disengage` を噛みつき後の反動・離脱状態として物理更新に組み込んだ
- Unit test で deterministic、停止時 jitter、heading alignment、turn limit、contact、retreat、pheromone/objective、collision、integration、ant-likeness thresholds を検証した
- Playwright/verify で agent battle の生成、物理ログ、replay agent 表示、診断ログを検証した

## 2026-06-24: 遠征戦闘を既存アリ個体へ接続

### Decision

遠征開始時に別 renderer の戦闘用アリを重ねるのではなく、通常画面にいる既存 `Ant3D` を選抜し、その id、位置、向き、歩行位相、描画slotを agent battle の初期状態へ渡す。戦闘 frame log は同じ `Ant3D` へ適用し、通常 `AntRenderSystem` で描画する。

### Result

- agent battle は `playerSeeds` を受け取り、既存個体の id/position/heading/gaitPhase/renderIndex/spawnReason を初期frameに保持する
- `AntRenderSystem` は id 由来の stable slot を持ち、戦闘中に不要な render index swap を起こさない
- 遠征中の参加個体は通常更新を止め、agent frame log で同じインスタンスを制御し、終了時に負傷/撤退/復帰を同じ個体へ反映する
- 追加の敵個体は通常 renderer に流す軽量 visual とし、別 expedition renderer は生成しない
- `src/expedition/qa/AntBattleInspector.ts` を追加し、identity/teleport/二重描画/状態異常/perf 兆候を simulation へ副作用なく report する
- Unit/Playwright/verify で transform continuity、renderIndex 継続、Inspector 診断、legacy flag の分離を検証する

## 2026-06-24: 遠征挙動だけを確認するモードを追加

### Decision

通常プレイや保存データを変えずに遠征挙動だけを確認できるよう、URL パラメータ `?expeditionOnly=1` を確認モードとして扱う。確認モードでは保存データを読まず、localStorage にも書かず、一時的なコロニー状態で遠征タブを開き、自動で agent 遠征を開始する。

### Result

- `?expeditionOnly=1` または `?mode=expedition` で遠征確認モードに入る
- 成長タブと資源バーを隠し、遠征タブと戦闘ログだけを前面に出す
- 遠征終了後は短い間隔で次の遠征を再開できるようにし、連続観察しやすくした
- 確認モードでは `saveColony()` を無効化し、既存セーブデータを汚さない
- Playwright で自動開始、参加個体、敵visual、critical diagnosticsなし、保存非書き込みを検証する

## 2026-06-24: 敵アリを常駐から予告付き定期襲撃へ変更

### Decision

敵アリが巣の周辺や餌場に最初から常駐していると、プレイヤーから見て「なぜそこにいるか」が読みにくい。通常盤面の敵アリは、防衛ゲーム寄りの定期襲撃として扱い、襲撃前に予兆を出してから外縁から侵入させる。

### Result

- 初期状態と襲撃待機中は通常盤面の敵アリを0匹にした
- `raidState` をセーブデータへ optional 追加し、旧セーブでは待機状態を補完する
- 保存中に active/retreating だった襲撃は、再読み込み時に warning へ戻し、敵が突然近くに出る状態を避ける
- 襲撃は `calm -> warning -> active -> retreating/recovering -> calm` のサイクルで進む
- warning では外縁に警戒フェロモンとログを出し、active で最大4匹が外縁から侵入する
- 敵脅威は襲撃間隔、敵数、襲撃敵の強さへ小さく反映する
- 巣の防衛値は交戦時の味方防衛力と襲撃間隔へ反映し、防衛強化の理由を読みやすくする
- 敗北や防衛失敗時の被害は小さめの食料減少と少数負傷に抑え、放置成長の主軸を壊さない

## 2026-06-24: 襲撃戦闘を集団交戦と死亡コストへ寄せる

### Decision

敵アリが少数で出てきて、接触しても味方が逃げるだけでは、防衛ゲームとして危機感が弱い。アリ同士の戦闘は、単純な1対1の正面押し合いではなく、複数個体が絡む fighting group、局所的な警戒、死亡を含むコストとして表現する。

### Basis

- 実験ベースのアリ戦闘モデルでは、単体個体だけでなく「2対1」などの fighting group を状態として扱っている
- アリの危険情報は短距離では警戒や突撃を生むが、長距離へ完全に共有されるとは限らず、結果として個体の犠牲が発生しうる
- アリの戦争や縄張り争いは、種や状況によって多数個体、兵隊個体、死亡・負傷個体の回収を伴う

Sources:
- https://arxiv.org/abs/1503.06094
- https://arxiv.org/abs/1306.3077
- https://arxiv.org/abs/1512.04162
- https://en.wikipedia.org/wiki/War_in_ants

### Result

- 襲撃規模を最大10匹、初期脅威ではおおむね6匹に増やした
- 敵1匹との接触は、近くのガードや働きアリが加わる集団交戦へ変更した
- 交戦中の味方は敵の正面に一列で貼り付かず、横や斜めから囲む配置にした
- 敵が勝つと味方個体が死亡し、`antPopulation` と `fallenAnts` に反映する
- 味方が十分な数で囲むと敵個体を制圧し、襲撃側の損耗として扱う
- 敵が巣周辺や餌場に居座ると、接触戦闘がなくても食料被害と低確率の死亡が発生する
- `raidState` に casualties / enemyCasualties / breachTimer を optional 追加し、旧セーブでは0へ補完する

## 2026-06-25: 交戦中の固まり感を描画で緩和

### Decision

接敵時に個体がその場で組み合う方針は維持する。ただし、静止して見えると戦闘ではなく描画停止に見えるため、戦闘中だけ脚・触角・大顎の位相アニメーションと、砂煙、衝突リング、短い噛み合いフラッシュを出して「その場で争っている」状態を表現する。

### Result

- 交戦中も味方アリと敵アリの歩行位相を進め、脚が固まらないようにした
- appendage 描画で脚、触角、大顎を状態に応じて微動させた
- `combatEffects` を保存されない一時描画として追加し、戦闘中だけ砂煙・リング・フラッシュを生成する
- エフェクトは最大96個で古いものから破棄し、リセット/終了時にも破棄する
- Playwright と verify で、交戦中の gait 進行と combat effect 発生を検証対象にした

## 2026-06-25: 戦闘後の残り方と襲撃隊形を自然化

### Decision

戦闘終了後の敵は、その場で固まったり突然消えたりさせない。制圧された敵は死体としてその場に残し、退却する敵は外周の内側ではなく画面外へ逃げる。襲撃開始時は外周円弧へ横一列に並べるのではなく、同じ進入方向を保ちながら前後差と横ずれを持つ緩い扇形で侵入させる。

### Result

- 撃破された敵は `defeated` として扱い、ライブ個体から外して死体 mesh を残す
- 退却する敵のホーム地点はワールド半径の外に置き、画面内で固まらないようにする
- 働きアリ、運搬中、帰巣中、餌場近く、巣から離れた個体を敵の攻撃対象として明示的に優先する
- 襲撃スポーン位置に前後差と横ずれを入れ、目標地点も個体ごとにずらした
- Playwright と verify で、出現位置と目標地点のばらつき、退却先が画面外であること、撃破後に死体が残ることを確認する

## 2026-06-25: 働きアリ攻撃と死体視認性を強化

### Decision

敵襲中の敵アリは、餌だけでなく、巣外で採餌・運搬・帰巣している働きアリを優先的に攻撃する。撃破された敵は、ライブ個体が消えたように見えないよう、胴体だけでなく脚と触角を含む低い死体としてその場に残す。

### Result

- 襲撃敵の働きアリ検知範囲を広げ、露出、運搬中、帰巣中、餌場近くの worker に強い優先度を付けた
- 襲撃戦闘では周辺個体が加勢しやすいように recruit 範囲を少し広げ、worker が単独で捕まるだけの場面を減らした
- 敵死体の色、接地跡、脚、触角を強め、戦闘直後と数秒後のスクリーンショットで残存確認できるようにした
- `npm run verify:combat` を追加し、離れた worker を標的にすること、戦闘中の組み合い、戦闘後の死体残存を固定シナリオで確認する

## 2026-06-25: 通常モードの敵襲確認URL

### Decision

通常モードの敵襲挙動確認には `?raidSoon=1` を使う。`?raidSoon=1` は通常UIと通常更新を維持したまま、敵襲の待機時間と予兆時間だけを短くする。

### Result

- 通常URLの敵襲間隔やバランスは変更しない
- `?raidSoon=1` では通常の成長/敵襲更新を動かす
- 確認中の短縮タイマーや戦闘結果で通常セーブを汚さないよう、`raidSoon` 中は保存を抑制する
- Playwrightで通常モードのまま短時間で active raid に入ることと、localStorageへ保存しないことを検証する

## 2026-06-25: 戦闘後の死体寿命と兵隊アリの迎撃

### Decision

死体はライブ個体と同程度のサイズで地面に残す。戦闘結果が読めるだけの時間は残すが、放置画面に蓄積して視認性を落とさないよう10秒で自然に消える。防衛中の兵隊アリは、巣の近くで待つだけでなく、近くに入った襲撃敵へ迎撃に向かう。

### Result

- 敵死体と味方死体の表示スケールを、生前のアリ表示スケールに合わせた
- 敵死体と味方死体を10秒で破棄し、描画オブジェクトも解放する
- 味方アリが死亡した場合も、敵と同じく戦闘地点に死体を残す
- active/retreating の敵襲中、兵隊アリは近距離の敵脅威を検知して迎撃に向かう
- Playwright と `verify:combat` で、兵隊アリの迎撃、味方死体の発生、敵味方死体の10秒消滅、敵死体サイズを検証する

## 2026-06-25: 遠征モードを削除し兵隊タブへ置換

### Decision

遠征モードは通常ゲームの中心体験から外す。兵隊アリは平時に地表を巡回せず、巣内にいる数値リソースとして扱う。プレイヤーが兵隊タブから出撃を指示したときだけ、兵隊が巣口から地表に出て防衛行動を行う。

### Result

- 管理パネルの遠征タブを兵隊タブに置き換えた
- 遠征確認モード、遠征開始処理、遠征replay、遠征engine import を通常アプリから削除した
- 通常生成される盤面アリから guard role を外し、兵隊は巣内待機扱いにした
- 兵隊タブの出撃ボタンで最大12匹まで guard 個体を巣口から一時出撃させる
- 出撃兵は敵が見えていれば敵個体へ、まだ接近予兆だけなら敵襲シグナル方向へ前進する
- 出撃兵は通常の近距離警戒より広い範囲で敵を検知し、外縁から来る敵へ向かう
- 出撃時間切れ、または敵襲終了後は巣へ帰還し、地表表示から外れる
- Playwright と verify で、平時の兵隊非表示、兵隊タブ出撃、出撃後の迎撃、帰還を検証する

## 2026-06-25: 味方デフォルト色固定と3対1戦闘と敵襲通知

### Decision

ライブ状態の味方アリは、警戒・逃走・戦闘中でも常にデフォルト色で描画する。敵アリは戦闘中でも敵専用材質で描画する。戦闘は正面1対1だけでなく、最大3匹の味方が正面・側面・背面寄りから敵に噛み付く fighting group として見せる。敵襲の接近、開始、撃退は兵隊タブを開いていなくても分かるよう、画面上の一時通知とログの両方で知らせる。

### Result

- 味方アリは render state に関係なく `explore` 材質で描画する
- 敵アリは clash 中でも敵専用材質で描画し、味方側の戦闘材質を参照しないようにした
- 襲撃戦闘では最大3匹が加勢でき、3匹時は正面・側面・背面寄りの固定スロットへ配置する
- 敵襲予兆、敵襲開始、撃退/被害結果を `raidNotice` で数秒表示し、battle log にも従来通り残す
- 撃退/被害表示の死亡数は `raid.casualties` 単体ではなく、敵襲開始時の `fallenAnts` との差分から算出する
- Playwright と verify で、味方デフォルト材質固定、3対1の取り付き角度、敵襲/撃退通知、死亡数表示を検証する

## Current Direction

今後の判断では、以下を優先する。

- 放置成長の気持ちよさ
- 小さな巣が大帝国になる長期目標
- 盤面上の自律行動による観察価値
- スマホで読める UI
- 不快すぎないアリ表現
- 強化、兵隊出撃、外敵、防衛がつながるゲームループ

## 2026-06-26: mainの敵襲/兵隊出撃更新へ重兵装アリと土木アリを統合

### Decision

main側で遠征が兵隊出撃と敵襲防衛へ置き換わったため、重兵装アリは出撃リソースではなく巣内防衛/前線維持のvariantとして扱う。土木アリは働き蟻枠から分かれる作業variantとして、地形変形ではなく軽量なEarthwork範囲効果だけを作る。

### Result

- save versionを5へ上げ、`heavySoldierAnts`、`builderAnts`、`nextEarthworkId` を既存saveへ後方互換で追加する
- `AntVariant` とvariant configを追加し、速度、旋回、耐久、押し合い、採餌効率、維持費、危険反応を定数で参照する
- 重兵装アリは通常兵隊より遅く高コストだが、brace/push/blockと巣周辺防衛に強い。当初は兵隊出撃数に含めなかったが、2026-06-27に通常兵隊と同じオンデマンド出撃へ統合した
- 土木アリは土粒を運び、`trailReinforce` と `lowBarricade` を進め、敵接近時は作業を中断して退避する
- Earthworkは味方移動/生産、敵速度、重兵装braceへ小さく作用するだけにし、地形編集は入れない
- mainのid→renderIndex管理、兵隊出撃、敵襲通知、combat outcomeの流れを維持したまま、variant描画と土粒描画を追加する

## 2026-06-26: 土木工事をユーザー発注型に変更し完成物を可視化

### Decision

巣の成長強化とは別に、土木工事は土木タブからユーザーが発注する。土木アリが自動需要だけで作るのではなく、プレイヤー指示があってから `trailReinforce` / `lowBarricade` タスクを作る。完成物は人工的な道路や壁ではなく、踏み固め土と土粒の堆積で見せる。

### Result

- 土木タブ、土木アリ数、作業中数、完成数、2つの発注ボタンを追加
- `ensureBuildTasks` は自動生成をやめ、発注済みタスクの整理だけにした
- `trailReinforce` は薄い土色の踏み跡と小さな土粒列で表示する
- `lowBarricade` は巣口や脅威方向の低い半月状土盛りで表示する
- Earthworkをsave version 6で保存/復元し、途中作業も再開できる
- Playwrightで土木タブ発注、完成物効果、save/load migrationを検証する

## 2026-06-26: 土木工事の進捗と担当を明示

### Decision

土木工事は、1つの工事へ全土木アリを自動投入するのではなく、まずは1タスクにつき1匹が担当する小さな作業単位として扱う。プレイヤーが進行理由を読めるよう、土木タブに工事ごとの進捗率、担当数、土木アリの状態内訳を表示する。

### Result

- 土木タブに工事ごとの進捗バーと `担当 0/1` / `担当 1/1` を追加
- 土木アリの状態を `採土`、`運搬`、`作業`、`退避`、`待機` に分けて表示
- 敵接近で土木アリが退避する時は、タスクの担当を解除し、他の土木アリが引き継げるようにする
- 自動的に全員で同じ工事へ集中する仕様は入れず、複数工事がある時に分散できる形を維持する

## 2026-06-26: 食料収入を3倍に緩和

### Decision

現状バランスがシビアなため、食料収入速度を3倍にする。アリが餌を持ち帰った時の実収入と、画面表示およびオフライン成長で使う `foodRate` の両方に同じ倍率をかける。

### Result

- `FOOD_INCOME_MULTIPLIER = 3` を追加
- 帰巣時の食料獲得量を3倍にする
- `foodRate` と不在中の食料増加も3倍にする
- 強化ツリー内の相対的な採餌強化倍率は維持する

## 2026-06-27: 土木工事に複数担当を許可

### Decision

土木工事は、1つの工事へ複数の土木アリが同時に参加できるようにする。無制限に密集させるのではなく、まずは1工事あたり最大3匹までの担当枠にする。

### Result

- 土木タスクの担当を単独の `claimedBy` から複数の `claimedByIds` へ拡張
- UIの担当表示を `担当 3/3` のように複数枠対応へ変更
- 複数の土木アリが同じ工事で作業すると、各個体の作業分だけ進捗が加算される
- 退避や完成時は、同じ工事の担当枠から該当個体または全担当を解除する

## 2026-06-27: 特殊アリに追従ラベルを追加

### Decision

土木アリ、重兵装アリ、出撃兵隊アリは、通常働きアリと見分けやすいようにロゴ付きの小さな追従ラベルを表示する。アリ本体の色は状態に関係なく既存方針を維持し、ラベルは本体InstancedMeshとは別のSpriteとして扱う。

### Result

- 画像生成で土木、重兵装、兵隊のロゴを作成し、透明PNGとして `public/assets/generated/` に保存
- CanvasTextureでロゴと短い日本語ラベルを合成し、Three.js Spriteで特殊アリに追従表示する
- ラベルは敵アリと通常働きアリには表示しない
- 既存のid、renderIndex、アリ本体描画には手を入れず、別レイヤーの軽量表示に留める

### Follow-up

兵隊、重兵装、土木のロゴ色が近く見えるため、ラベル側に落ち着いた役割別パレットを追加する。兵隊はくすんだ黄土、重兵装は赤錆、土木は苔混じりの土色を使い、アリ本体の色や状態色は変更しない。

## 2026-06-27: 土木アリの巣内待機と工事分散

### Decision

土木アリは工事担当になった時だけ巣外へ出る。未割当時に地表を探索させると、通常働きアリとの差が曖昧になり、プレイヤーの「発注したから出てきた」という因果も弱くなるため、巣内リソースとして非表示待機させる。

土木アリの数は `builderTraining` 1段階あたり1匹から2匹へ増やす。工事は1つへ全員が寄るのではなく、担当数が少ない工事を優先して割り当て、待機中の土木アリがいる限り別種類の工事を同時に進められるようにする。

### Result

- `builderTraining` 1段階あたりの土木アリ上限を2匹に変更
- 未割当の土木アリは巣内座標へドックし、地表描画と追従ラベルから除外
- 工事開始時は、既存工事の最低担当枠を考慮して待機土木アリが残る場合だけ別工事を発注可能
- 工事割当は担当数が少ないタスクを優先し、複数種類の工事へ自然に分散する

## 2026-06-27: 巣を地面の穴として描画し、帰巣後は10秒隠す

### Decision

巣の見た目は盛り上がった蓋状のドームではなく、地面に開いた暗い穴と縁、掘り出し土だけで表現する。帰巣したアリが即座に外へ反転して出ると「穴に入った」感覚が弱いため、穴に入った味方アリは一時的に巣内状態へ移し、最低10秒は地表に表示しない。

### Result

- 巣の球状マウンドを廃止し、地面上の暗い楕円穴と低い縁に変更
- 帰巣して食料を渡した通常アリは `inNest` / `nestStayTimer` で10秒間非表示
- 巣内アリはレンダー、ラベル、選択、敵の標的、石ショック、救助候補から除外
- 巣内滞在はセーブしない一時状態として扱い、既存セーブ互換性は維持

## 2026-06-27: 兵隊アリの同時出撃数を総数の半分に制限

### Decision

兵隊タブから一度に出撃できる通常兵隊アリは、通常兵隊アリ総数の半分までにする。奇数の場合は端数を切り上げ、総数1匹の序盤でも最低1匹は出撃できるようにする。全兵隊を一度に地表へ出すと巣内防衛リソースの意味が弱くなるため、プレイヤー指示の手応えは残しつつ、巣内に控えを残す。

### Result

- 固定上限 `12` ではなく `ceil(normalSoldiers / 2)` を同時出撃上限に変更
- 既に出撃中の兵隊数を差し引き、同時出撃数が半数上限を超えないようにした
- 兵隊タブの出撃ボタン表示と実際の出撃数を同じ `plannedSortieCount()` で統一

## 2026-06-27: 巣入口穴の直径を10分の1に縮小

### Decision

巣入口の黒い開口が大きく見えすぎていたため、中央穴と周辺入口の見た目の直径を現在値の10分の1にする。巣の位置、掘り出し土、帰巣後10秒非表示の挙動は変えない。

### Result

- 穴の黒い面と縁の描画スケールに `NEST_HOLE_DIAMETER_SCALE = 0.1` を適用
- 初期シーン smoke で中央穴と入口穴の最大直径を確認する検証を追加

## 2026-06-27: 出撃ウェーブと敵襲規模を巣規模へ対応

### Decision

兵隊アリの「半分まで出撃」は同時上限ではなく、1回の出撃指示で出せるウェーブ上限として扱う。クールダウン後は巣内に残る兵隊を追加出撃できる。重兵装アリも通常兵隊と同じ巣内兵力に統合し、平時は地表に出さず、プレイヤー指示時だけ出撃させる。

大規模な巣でも敵襲が少数固定だと防衛の緊張が消えるため、敵襲数は `enemyThreat` に加えて有効アリ数、巣Lv、領土、兵隊構成から伸びるようにする。ただしモバイル描画負荷を避けるため、まずは上限40匹に留める。

### Result

- 出撃上限を通常兵隊+重兵装アリ合算の `ceil(total / 2)` に変更
- 出撃中の数は総兵力から差し引くが、半数ウェーブ上限自体はクールダウン後に再利用できる
- 重兵装アリは平時の地表個体から外し、出撃時にだけ重兵装variantとして生成する
- 500匹級の巣では敵襲数が30匹以上まで伸びるようにし、Playwrightで規模連動を確認する
- 土木工事ボタンへ、効果とおおよその建築時間を短く表示する
- 大規模敵襲を含むmobile smokeが長くなるため、eval serverのTTLを15分へ延長する

## 2026-06-27: 土木工事ボタンの時間表現を修正

### Decision

土木工事ボタンに表示していた「約3秒」「約4秒」は、内部のprogress量を単純に秒へ読ませる表現で、実際のプレイ時間ではなかった。土木アリは採土、往復、現地作業を繰り返し、距離と担当数で完了時間が変わるため、固定秒数の表示は避ける。

### Result

- 土木工事ボタンの補足を `採土・往復あり` に変更
- tooltipでは `完了時間は距離・担当数で変動` と説明する
- 工事ロジック、progress量、セーブ形式は変更しない

## 2026-06-27: 通常マップから枝障害物を削除

### Decision

枝は自然物としての見た目は良いが、細長い障害物判定にアリが引っかかり、放置観察中に動けなくなる場面が多い。観察の気持ちよさと採餌の安定性を優先し、通常マップの固定枝配置を削除する。

### Result

- `seedNaturalObstacles()` で固定枝を生成しないようにした
- 既存の自然石、地形パッチ、餌場、土木オブジェクトは維持する
- 初期scene smokeとverify-renderは `branchCount === 0` を確認する
- セーブ形式、アリAI、枝追加用の休眠内部処理は変更しない

## 2026-06-27: 大きな土壁と建築コストを追加

### Decision

土木タブの建築対象を増やし、低い土塁より大きく、防衛寄りの `earthWall` を追加する。土木工事は内部progress量を固定秒数として見せず、建築コスト `buildCost` と所要目安を表示する。実際の完了時間は採土、往復距離、担当数で変動する。

### Result

- construction registryへ `earthWall` を追加
- `trailReinforce`、`lowBarricade`、`earthWall` に `buildCost` と `timeHint` を追加
- 土木ボタンと進行表示に工数と目安を表示
- `earthWall` は低い土塁より大きい視覚表現にし、敵減速と重兵装の踏ん張り支援を強める

## 2026-06-27: 土壁を配置指定できる防壁へ変更

### Decision

`earthWall` は単なる大きい土塁ではなく、敵の侵入を防ぐための細く高い防壁として扱う。プレイヤーは土壁ボタンを押した後、地表クリックで建築位置を指定する。リアリティよりゲーム性を優先し、壁付近の味方は壁上から攻撃し、敵は壁を越える時に大きく減速する。

### Result

- `earthWall` ボタン押下後に配置待ち状態を作り、canvasクリックで建築タスクを生成
- 壁は巣から見た指定地点の接線方向へ自動回転し、敵侵入を遮る向きにする
- `earthWall` の見た目を細く高い壁に変更
- 壁上の味方に攻撃ボーナス、壁付近の敵に強い移動低下を追加
- `lowBarricade` の既存減速/踏ん張り効果の上限は維持

## 2026-06-27: 土壁を線指定と長さ別コストに変更

### Decision

土壁は点配置ではなく、プレイヤーがドラッグした線分として指定する。壁の長さはセーブ形式を増やさず、既存の `radius` に半長相当として保存する。建築コストは固定値ではなく、指定した線の長さに比例して増減させる。

### Result

- `earthWall` 配置待ち中の pointer down/move/up で線分を指定
- ドラッグ中に壁プレビューと概算工数を表示
- 指定線分から中心、回転、長さ、`radius`、`maxProgress` を計算
- `earthWall` の保存互換性は維持し、既存の `x/z/radius/rotation/maxProgress` の範囲で表現

## 2026-06-28: 土壁の線指定中ガイドを追加

### Decision

土壁をドラッグ指定している間、プレイヤーが現在どの線を引いているかを地表上で判別できる必要がある。WebGL の線幅依存を避けるため、指定線は `THREE.Line` ではなく細いメッシュとして描画する。

### Result

- 土壁の半透明プレビューに、黄色の中心線と両端マーカーを追加
- ガイド線はドラッグ中のみ表示し、発注確定またはキャンセル時に破棄
- 建築コスト、保存形式、土壁の戦闘効果は変更しない

## 2026-06-28: 見張り塚と敵襲方角の情報制御を追加

### Decision

防衛建築が土壁で十分強くなったため、次の建築は敵を直接弱体化するものではなく、予兆段階の判断材料を増やす `sentryMound` とする。見張り塚がない場合、敵襲の方角は予兆段階では見えない。見張り塚が完成している場合だけ、方角シグナルを出し、兵隊をその方角へ前方布陣させる。

### Result

- construction registryと土木UIへ `sentryMound` を追加
- 完成済み見張り塚があると敵襲警告時間が少し延びる
- 見張り塚なしの予兆では方角アラームと兵隊の予兆中ターゲットを出さない
- 見張り塚ありの予兆では方角アラームを出し、出撃兵が敵襲方向の前方ラインへ布陣する
- セーブ形式は既存の `earthworks` の `kind/x/z/radius/progress/maxProgress/rotation` の範囲で維持

## 2026-06-27: 土木工事 registry を追加

### Decision

土木対象の追加時に `simulation.ts` へ条件・表示・進捗値が散らばらないよう、工事定義を `src/config/construction.ts` に集約する。現行の `trailReinforce` と `lowBarricade` の挙動、表示文言、セーブ互換は維持する。

### Result

- `CONSTRUCTION_DEFS` から半径、完了量、上限、開始/完了ログ、tooltip説明を参照する
- old/malformed save の工事kindは registry 経由で安全に正規化する
- construction registry の unit test を追加し、既存の土木工事2種が registry と同期していることを確認する

## 2026-06-27: 酸射アリを追加

### Decision

酸射アリは兵隊系特殊アリとして扱い、平時は巣内兵力に含める。プレイヤーが兵隊タブから出撃した時だけ地表へ出し、短射程で停止して腹部を持ち上げ、敵へ酸をかけて一時的に速度と戦闘力を下げる。味方アリ本体の色は状態に関係なく既存のデフォルト色を維持する。

### Result

- `acidShooter` variant と `acidShooterBrood` upgrade を追加
- save version 7 で `acidShooterAnts` を保存し、old save では 0 に移行する
- 酸射は通常兵隊・重兵装アリと同じ出撃プールに入り、1回あたり半数出撃ルールを共有する
- 酸攻撃は `acidDebuff` と酸エフェクトを付与し、即死や既存の組み合い戦闘時間は変更しない
- Playwright smoke/save と combat verify で、巣内待機、出撃、酸デバフ、セーブ移行、戦闘描画を確認する

## 2026-06-27: 酸射アリの接触戦移行を抑制

### Decision

酸射アリが初弾後すぐ通常の組み合い戦闘へ吸われていたため、酸射を主行動として見せる方向へ小さく調整する。通常兵隊や重兵装アリの組み合い戦闘、敵襲の基本処理、セーブ形式は変更しない。

### Result

- 酸射の射程、再射撃間隔、デバフ時間を酸攻撃が継続して見える値へ調整
- 酸射アリは射撃直後は停止し、再射撃待ちで近すぎる時だけ距離を取り直す
- 酸を受けた敵は再射撃前に通常接触戦へ入りにくいよう、短い接触抑制を持つ
- smoke testで酸射アリが複数回酸を撃ち、通常の組み合いへ入らないことを確認する

## 2026-06-27: 盾頭アリを追加

### Decision

盾頭アリは兵隊系特殊アリとして扱い、平時は巣内兵力に含める。重兵装アリのように前線で押し合う役ではなく、出撃時に巣口や狭い通路のブロック地点へ移動して停止し、敵の侵入速度と巣周辺被害圧を下げる防衛役にする。味方アリ本体の色は変えず、大きく平たい頭部と盾状プレートで見分ける。

### Result

- `shieldHead` variant と `shieldHeadBrood` upgrade を追加
- save version 8 で `shieldHeadAnts` を保存し、old save では 0 に移行する
- 出撃プールは通常兵隊、重兵装、盾頭、酸射の合算にし、1回あたり半数出撃ルールを共有する
- 盾頭アリは追撃せず、`shieldHeadBlockPoint()` で決めた巣口寄りの地点に構えて `rivalSpeedAt()`、`braceBonusAt()`、巣周辺被害圧へ軽い範囲効果を出す
- Playwright smoke/save と verify で、巣内待機、出撃、ブロック行動、セーブ移行、描画メッシュを確認する

## 2026-06-27: 盾頭アリを非戦闘の押し返し役へ調整

### Decision

盾頭アリが通常の組み合い戦闘に参加すると重兵装アリとの差が見えにくいため、盾頭は戦闘を開始せず、敵の接触時に構えたまま押し返す防衛壁として扱う。味方を守る役割は、盾頭の近くにいる味方を敵の標的にされにくくする cover と、敵の短い接触抑制で表現する。

### Result

- `shieldHead` は `startRivalClash()` と追加grappler募集から除外する
- 敵が盾頭に接触した時は `shieldPush` として敵を巣の外側へ押し返し、短い `fightCooldown` と `disrupt` を付与する
- 盾頭近くの味方には `shieldCoverStrengthAt()` による標的優先度低下を付ける
- 盾頭の頭部はより薄く横広い板状に調整し、ぼってりした丸みを抑える

## 2026-06-27: 酸射アリの酸を赤い飛翔エフェクトへ調整

### Decision

酸射アリの攻撃が見えにくいと通常戦闘との差が弱いため、酸の見た目を赤系に寄せ、発射元から敵までの軌跡に粒子列を出す。酸の射程、デバフ、接触戦抑制などのゲームロジックは変更しない。

### Result

- 酸ビームを赤橙、着弾飛沫を赤紫系に変更
- 酸の飛翔経路に赤いdroplet列を追加し、短時間フェードさせる
- smoke testで酸エフェクト色、droplet数、ビーム太さを確認する

## 2026-06-27: 盾頭アリを前線タンクへ調整

### Decision

盾頭アリが巣口付近に留まると、兵隊タブから出撃させた時に前線を支える個体として見えにくい。出撃中の盾頭は敵や敵襲シグナルの手前まで進み、味方の前で構えるタンク役にする。通常の組み合い戦闘には戻さず、押し返しとcoverで前線維持を表現する。

### Result

- `shieldHeadBlockPoint()` を巣口固定ではなく、現在の敵/敵襲シグナルの手前に置く前線配置へ変更
- 敵がさらに奥へ動いた場合、盾頭も前線ブロック地点へ再移動する
- smoke testで盾頭が巣口より前に出て、敵より味方側で構え、前線追従の移動意図を持つことを確認する

## 2026-06-28: 斥候アリを追加

### Decision

重兵装、盾頭、酸射で前線/封鎖/短射程デバフは揃ってきたため、次の特殊アリは火力追加ではなく戦線整理の支援役にする。斥候アリは兵隊系特殊アリとして巣内兵力に含め、プレイヤーが兵隊タブから出撃した時だけ地表へ出る。敵とは組み合い戦闘を発生させず、長い触角で敵を標識し、味方兵と酸射アリが同じ敵を優先しやすくする。

### Result

- `scout` variant と `scoutBrood` upgrade を追加
- save version 9 で `scoutAnts` を保存し、old save では 0 に移行する
- 斥候は通常兵隊、重兵装、盾頭、酸射と同じ出撃プールに入り、1回あたり半数出撃ルールを共有する
- 標識された敵は `findRivalThreat()` で優先されやすくなり、酸射や出撃兵の狙いが揃いやすくなる
- 斥候は敵接触時に組み合い戦闘へ入らず、短く退避する
- smoke/save/unit/verifyで、巣内待機、出撃、標識、セーブ移行、描画ラベルを確認する

## 2026-06-28: 小隊長アリMVPを追加

### Decision

出撃兵がバラバラに戦うと役割連携が見えにくいため、小隊長アリを兵隊系特殊アリとして追加する。MVPでは永続的な小隊編成UIやセーブ対象の小隊データは作らず、プレイヤーが兵隊タブから出撃した時だけランタイム上で一時小隊を作る。小隊長は火力を大きく増やさず、盾頭を前、酸射や斥候を後ろ寄りに置く集合アンカーと、同じ敵を狙いやすくする目標共有で集団戦を表現する。

### Result

- `captain` variant と `captainBrood` upgrade を追加
- save version 10 で `captainAnts` を保存し、old save では 0 に移行する
- 小隊長は通常兵隊、重兵装、盾頭、酸射、斥候と同じ出撃プールに入り、1回あたり半数出撃ルールを共有する
- 出撃時に小隊長1匹あたり最大6匹の一時小隊を作り、帰還・死亡・退場時にランタイム状態として破棄する
- 小隊メンバーは既存AIを維持しつつ、`squadTargetId` と `squadAnchor` による軽い目標/隊列補正を受ける
- smoke/save/unit/verifyで、巣内待機、出撃、隊列アンカー、集中目標、セーブ移行、描画ラベルを確認する

## 2026-06-28: 小隊リングを小隊色で表示

### Decision

小隊長の指揮範囲と麾下メンバーの所属が見えにくいため、小隊ごとにランタイム色を割り当てる。味方アリ本体色は既存ルール通り変えず、足元リングと指揮パルスだけを小隊色にする。小隊色は出撃時に作る一時小隊の表示状態であり、セーブ対象にはしない。

### Result

- 小隊長の指揮パルスを大きく、長め、濃いめに調整
- 出撃時に各小隊へ `colorHex` を割り当て、小隊長と麾下メンバーに同じ `squadColorHex` を持たせる
- `SquadRingSystem` で小隊長と麾下メンバーの足元に同色リングを表示する
- smoke testで、1小隊内の同色表示と複数小隊の別色表示を確認する

## 2026-06-28: 小隊結束と斥候の前線標識を調整

### Decision

小隊長の隊列がすぐ広がると集団戦の利点が見えにくく、斥候が後方に残ると敵位置を知らせる役割が弱く見える。小隊は隊列間隔を少し詰め、アンカーへ戻るステアリングを強める。斥候は敵と組み合わないルールを維持しつつ、標識可能距離の外縁で停止せず、前線付近まで寄りながら敵を標識する。

### Result

- 小隊メンバーの横幅と前後差を圧縮し、集合ステアリングを強化
- 斥候の出撃中の敵回避圧を下げ、標識中でも前線距離や小隊アンカーへ寄れるよう調整
- 斥候の安全距離を短くし、敵位置を知らせる前線支援役として見えやすくした
- smoke testで、斥候が標識しながら前線へ寄ることと、小隊内の斥候アンカーが小隊長より敵側に置かれることを確認する

## 2026-06-28: 小隊長を囲む隊列へ調整

### Decision

小隊長が先頭で敵へ寄りすぎると、指揮役ではなく前衛として倒れやすく見える。小隊長は小隊の中心で指揮し、盾頭や重兵装が前、酸射が後ろ、通常兵や斥候が左右を固める配置に寄せる。小隊長ごとの色分けは小隊色として維持し、小隊長自身と麾下の足元リング、指揮パルスに同じ色を使う。

### Result

- 小隊アンカーを小隊長中心の囲み型に調整
- 小隊結束が低い時、小隊長の前進を抑えて味方を待つ
- 敵に近すぎる小隊長は少し下がり、指揮距離を保ちやすくした
- smoke testで、小隊長の前後にメンバーが配置されること、低結束時に小隊長が突っ込まないこと、小隊長ごとのリング色が分かれることを確認する

## 2026-06-28: 小隊色と盾頭前線位置を強調

### Decision

小隊リング色が淡い近似色だと、複数小隊が出た時に所属差が読み取りにくい。小隊色は砂地上で見分けやすい高彩度色へ寄せる。盾頭アリはタンク役としてさらに前線に出し、敵との接触直前の位置で押し返しと cover を発揮しやすくする。

### Result

- 小隊色を赤、青、緑、黄、紫、橙の高彩度パレットへ変更
- 小隊長リングの白寄せを弱め、各小隊色の差を残す
- 盾頭アリの敵前ブロック距離を短くし、敵に近い前線まで出るよう調整
- smoke testで、小隊長ごとの色距離と盾頭ブロック地点が敵近くに置かれることを確認する

## 2026-06-28: 小隊リングを太い色帯へ変更

### Decision

小隊ごとの `colorHex` は分かれていても、細い足元リングでは通常カメラ距離と砂地の上で色差がほぼ見えない。小隊所属はプレイヤーが一目で読める必要があるため、足元表示を細いトーラスから平面リングへ変更し、深度テストを切ってユニットや地面に埋もれないようにする。

### Result

- `SquadRingSystem` のリングを見やすい `RingGeometry` に変更
- 小隊長リングと麾下リングのサイズを拡大し、不透明度を上げた
- 小隊色の白寄せを抑えたまま、複数小隊で赤/青/緑などが視認できるように調整
- smoke testで、小隊リングの不透明度、depthTest設定、帯幅を確認する

## 2026-06-28: 小隊の兵科配分と目標分散を追加

### Decision

小隊が複数出た時に同じ兵科だけが一部小隊へ偏ったり、全小隊が同じ敵へ重なったりすると、集団戦の見た目と役割分担が弱くなる。出撃時の小隊編成は兵科ごとに小隊間へ均等配分し、敵が複数いる場合は既に他小隊が狙っている敵へペナルティをかけ、各小隊の進路も横方向に少しずらす。小隊長の「小隊内の集中目標」は維持するが、複数小隊全体ではばらける。

### Result

- 小隊メンバー割り当てを出撃順の詰め込みから兵科別バランス配分へ変更
- 複数小隊時は `laneOffset` によって rally 位置を横にずらし、小隊間隔を保ちやすくした
- 小隊目標選択で他小隊の目標にペナルティを付け、敵が複数いる場合に別目標へ分散しやすくした
- 小隊リングの線幅を前回の半分にし、色分けは維持しつつ画面占有を抑えた
- smoke testで、兵科配分、別目標選択、rally間隔、リング幅を確認する

## 2026-06-28: 標準難度の敵襲バランス検証を追加

### Decision

特殊兵種が増えたため、感覚だけで敵襲難度を判断せず、同じseed群で出撃あり/なし、混成、単一兵種、大規模巣を比較する。標準難度は、出撃と防衛投資で守れるが、未出撃や偏った兵種では被害が出る状態とする。

### Result

- `npm run verify:balance` を追加し、`verification/balance/summary.json` に5 seedの敵襲結果を出力する
- 指標は `deaths`、`enemyCasualties`、`foodLoss`、`woundedDelta`、`breachEvents` と `harmScore` で比較する
- 小隊長は指揮役としての位置づけを保つため、純戦闘値を `attack 0.44 -> 0.39`、`contact 0.34 -> 0.31` に下げた
- `raidEnemyCount()` で酸射アリも敵襲規模へ少し反映し、酸射投資だけが敵襲規模から漏れないようにした
- 検証結果は `early_sortie` 成功5/5・平均死亡1.2、`early_no_sortie` 平均harm 14.91、`mid_mixed` 成功5/5・完全無傷0、`late_pressure` 敵数17・平均harm 15.83

## 2026-06-29: 兵舎タブと兵種別育成キューを追加

### Decision

兵隊数が時間経過で自動的に目標値へ増えると、プレイヤーがどの兵種へ食料を使ったのか読み取りにくい。兵隊アリは平時に地表へ出さない既存ルールを保ち、巣内リソースとして兵舎タブで育成する。育成は先頭から順に進むキューとし、投入時に食料を支払い、完了時に1匹だけ増える。

通常兵は序盤から育成可能にする。重兵装、盾頭、酸射、斥候、小隊長は対応するBrood強化で解禁し、強化Lvごとの上限枠を持つ。既存の強化購入時に初回個体が入る挙動は互換のため残し、追加育成は兵舎キューに寄せる。

### Result

- 兵舎タブを追加し、兵隊アリ、重兵装アリ、盾頭アリ、酸射アリ、斥候アリ、小隊長アリを育成キューへ入れられるようにした
- 兵種ごとに食料コストと育成秒数を分け、完了時に巣内兵力へ1匹ずつ反映する
- 兵隊系の時間経過による自動補充をやめ、兵舎キュー完了を増加理由にした
- セーブversion 11で `barracksQueue` と `nextBarracksOrderId` を保存し、旧セーブは空キューとして移行する
- smoke/save/unit testでキュー投入、順次完了、保存互換を確認する
